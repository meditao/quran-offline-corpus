name: Build Amn audits

on:
  workflow_dispatch:
  push:
    paths:
      - '08_scripts/analyze_amn_verb_syntax.py'
      - '08_scripts/analyze_amn_bi_targets.py'
      - '08_scripts/analyze_iman_noun.py'
      - '08_scripts/analyze_mumin_profile.py'
      - '.github/workflows/build-amn-syntax.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build Amn verb syntax audit
        run: python 08_scripts/analyze_amn_verb_syntax.py
      - name: Build Amn bi-target audit
        run: python 08_scripts/analyze_amn_bi_targets.py
      - name: Build iman noun audit
        run: python 08_scripts/analyze_iman_noun.py
      - name: Build mumin profile audit
        run: python 08_scripts/analyze_mumin_profile.py
      - name: Commit generated audits
        run: |
          git config user.name "quran-offline-corpus-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 07_analyses/generated/root_profiles/Amn/verb_syntax
          git add 07_analyses/generated/root_profiles/Amn/bi_targets
          git add 07_analyses/generated/root_profiles/Amn/iman_noun
          git add 07_analyses/generated/root_profiles/Amn/mumin
          if git diff --cached --quiet; then
            echo "No Amn-audit changes."
            exit 0
          fi
          git commit -m "Build Amn syntax, bi-target, iman noun, and mumin audits"
          git push
